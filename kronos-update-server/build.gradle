version = '1.0'
project.ext {
    type = "server"
}

apply plugin: 'application'

dependencies {
    implementation project(':kronos-api')
}

application {
    mainClassName = 'io.ruin.update.Server'
}